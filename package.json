{
  "name": "@scriptollc/amqp-worker",
  "version": "1.3.0",
  "description": "Class for creating AMQP connections",
  "main": "amqp-worker.js",
  "scripts": {
    "test": "standard && npm run dep && tap --100 --cov --coverage-report=lcov test/*.spec.js test/*.integration.js",
    "dep": "dependency-check . && dependency-check . --unused --no-dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scriptoLLC/amqp-worker.git"
  },
  "keywords": [
    "rabbitmq",
    "amqp",
    "queue",
    "queue worker",
    "worker",
    "amqplib"
  ],
  "author": "Todd Kennedy <todd@selfassembled.org>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/scriptoLLC/amqp-worker/issues"
  },
  "homepage": "https://github.com/scriptoLLC/amqp-worker#readme",
  "dependencies": {
    "amqplib": "^0.5.2"
  },
  "devDependencies": {
    "dependency-check": "^3.0.0",
    "proxyquire": "^1.8.0",
    "standard": "^10.0.3",
    "tap": "^11.0.1"
  }
}
